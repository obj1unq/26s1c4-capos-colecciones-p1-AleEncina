object rolando {
    const mochila = []
    var limite = 2

    method encontar(artefacto) {
      if(self.hayEspacio()) mochila.add(artefacto) 
    }

    method hayEspacio() {
        return mochila.size() < limite 
    }

    method guardarEn(castillo) {
        mochila.addAll(castillo.almacen())
        //mochila.forEach({ artefacto => castillo.guardar(artefacto) })
        mochila.clear()
    }

    method mochilaVacia() {
      return mochila.clear()
    }

    method mochila() = mochila

    method limite(_limite) {
      limite =_limite
    }

    method castillo() {
      return castillo.almacen()
    }

    method polimorfismo() {
      return mochila.addAll(castillo.almacen())
    }

}

object castillo {
    const almacen = []

    method guardar(unElemento) {
      almacen.add(unElemento)
    }

    method almacen() {
      return almacen
    }

    method cantidad() {
      return almacen.size()
    }


}


object espada {

}


object libro {

}


object collar {
  
}

object armadura {
  
}


//capostest.wtest

import capos.*

describe "ronaldo"{

test encontrando con limite 2{
	rolando.encontrar(espada)
	rolando.encontrar(libro)
	rolando.encontrar(collar)

	assert.equals(#{espada,libro}, rolando.artefactos())
}

test 